# Sao chép file này thành ".env" (bỏ phần ".example") rồi điền API key thật vào.
# KHÔNG BAO GIỜ commit hoặc chia sẻ file ".env" thật - nó đã nằm trong .gitignore.
GEMINI_API_KEY=dán_api_key_của_bạn_vào_đây
